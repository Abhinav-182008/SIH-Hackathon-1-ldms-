# CaseVault frontend

See **../README.md** for startup, demo accounts, permission policy, verification, storage, Grok, and remaining cloud work.

Run the backend first, then `npm install` and `npm run dev` in this folder. Open http://localhost:5173. This UI requires the API; it no longer falls back to bundled credentials or browser storage.
